/// <reference types="node" />
import { ReadStream } from 'node:fs';
import type { FileReader, UploadInput } from '../options.js';
import BufferSource from './sources/BufferSource.js';
import StreamSource from './sources/StreamSource.js';
export default class NodeFileReader implements FileReader {
    openFile(input: UploadInput, chunkSize: number): Promise<{
        size: number;
        _stream: ReadStream;
        _path: string;
        slice(start: number, end: number): Promise<{
            value: ReadStream & {
                size?: number | undefined;
            };
            done: boolean;
        }>;
        close(): void;
    }> | Promise<BufferSource> | Promise<StreamSource>;
}
