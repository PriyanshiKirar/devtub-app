/// <reference types="node" />
import { type ReadStream } from 'node:fs';
import type { FileSource as IFileSource } from '../../options.js';
export default function getFileSource(stream: ReadStream): Promise<FileSource>;
declare class FileSource implements IFileSource {
    size: number;
    _stream: ReadStream;
    _path: string;
    constructor(stream: ReadStream, path: string, size: number);
    slice(start: number, end: number): Promise<{
        value: ReadStream & {
            size?: number | undefined;
        };
        done: boolean;
    }>;
    close(): void;
}
export {};
